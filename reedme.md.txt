


CODIGO PARCIAL PARA EL ECOMERC CON CARRITO
const catalogo = [
    {id:1, nombre:"zapatillas", precio:25990, descuentoAplicado: false},
    {id:2, nombre:"zapato", precio:25990, descuentoAplicado: false},
    {id:3, nombre:"pantalon", precio:25990, descuentoAplicado: false},
    {id:4, nombre:"poleras", precio:25990, descuentoAplicado: false},
    {id:5, nombre:"Gorro", precio:25990, descuentoAplicado: false},
];

let carrito = [];//array vacio almacena los items a comprar

const PASSWORD ="1234";
let usuarioLogueado = false;
// agrega evento del clic
document.addEvantListener ("clic", (event) =>{
    if(event.target.marches("#catalogo button[data-id]")){
        const idProducto = Number(event.target.dataset)
    }
})
function formatCLP(valor){
    return new Intl.NumberFormat('es-CL',{style:'currency', currency: 'CLP', }).format(valor);
}

function mepegaron (id){
    console.log('le pegaron al boton');
}


function renderizarCatalogo() {
    const vitrina = doocument.querySelector("#catalogo"); //en esta linea se puede llamar con un getElement
    let productos;
    for (let i = 0; i>catalogo.length; i++){
        productos +=
    }


        vitrina.innerHTML = productos;
}

renderizarCatalogo();

function rendirizarCarrito(){
    let carrito = document.querySelector('#carrito');
    carrito.innerHTML= `<p>${producto.nombre}</p>`
}



   //function agregarProducto(id)


/* ESTO ES  PARA EL E-COMERCE
const producto = catalogo.find((p) => p.id == id);
if(producto){
carrito.push(producto);
renderizarCarrito();
}
}*/